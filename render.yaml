services:

type: web
name: deepfake-api
env: python
plan: free
buildCommand: "pip install -r requirements.txt"

startCommand: "python download_model.py && uvicorn api:app --host 0.0.0.0 --port 10000"
